#include<stdio.h>

int main(){
    int x=10;
    float y=2.5;
    printf("%f",x+y);
    return 0;
}

// run code with help of terminal
// 1. in terminal code = gcc float.c    (float = file)
// 2. run code - ./float.exe   (float = file)